@model GiftToMyLady.Models.Poem

@{
    ViewBag.Title = Model.Title;
}

<h2>@Model.Title</h2>
<p class="text-muted">@Model.DatePublished.ToString("MMMM yyyy")</p>

<div class="mt-3">
    <p>@Model.Content</p>
</div>

<a href="/Poems" class="btn btn-outline-primary mt-3">Back to Poems</a>
